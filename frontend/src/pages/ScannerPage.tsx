import { RoutePlaceholder } from '@/shared/ui/RoutePlaceholder';

export function ScannerPage() {
  return (
    <RoutePlaceholder
      eyebrow="Основной раздел"
      title="Scanner"
      description="Полный список найденных сетапов, фильтрация, сортировка и переход в Workspace."
    />
  );
}
