name: Actions Smoke

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read

jobs:
  smoke:
    name: Runner echo
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "GitHub Actions runner started successfully"
